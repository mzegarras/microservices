server:
  port: 9000
spring:
  application:
    name: helloworld
  cloud:
    config:
      enabled: false
endpoints:
  shutdown:
    enabled: true
  restart:
    enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/